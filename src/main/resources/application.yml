spring:
  application:
    name: ms-resenas
  datasource:
    url: jdbc:mysql://localhost:3306/db_resenas?createDatabaseIfNotExist=true
    username: root
    password: Contrasena123
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8087

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

storage:
  service:
    url: lb://ms-storage   # unica URL que queda porque se utiliza webClient aqui